<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp"
        android:background="@color/surface_card"
        android:gravity="center_vertical"
        android:elevation="2dp">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_arrow_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="@color/text_main"
            android:contentDescription="@string/desc_back" />

        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp">

            <EditText
                android:id="@+id/etSearch"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:hint="@string/search_hint"
                android:inputType="text"
                android:background="@android:color/transparent"
                android:textColor="@color/text_main"
                android:importantForAutofill="no" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_gravity="end"
                android:orientation="horizontal">

                <ImageButton
                    android:id="@+id/btnClearSearch"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_close"
                    android:contentDescription="@string/desc_clear_text"
                    app:tint="@color/text_muted"
                    android:background="?attr/selectableItemBackgroundBorderless" />

                <ImageButton
                    android:id="@+id/btnMicSearch"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_mic"
                    android:contentDescription="@string/desc_voice_search"
                    app:tint="@color/action_blue"
                    android:background="?attr/selectableItemBackgroundBorderless" />
            </LinearLayout>
        </FrameLayout>
    </LinearLayout>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="12dp"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:orientation="horizontal">

            <TextView android:id="@+id/chipAll" style="@style/FilterChip" android:text="@string/chip_all" />
            <TextView android:id="@+id/chipWithImages" style="@style/FilterChip" android:text="@string/chip_images" />
            <TextView android:id="@+id/chipRecent" style="@style/FilterChip" android:text="@string/chip_recent" />
            <TextView android:id="@+id/chipOldest" style="@style/FilterChip" android:text="@string/chip_oldest" />
        </LinearLayout>
    </HorizontalScrollView>

    <TextView
        android:id="@+id/tvResultCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/text_muted"
        android:paddingHorizontal="20dp"
        android:paddingVertical="12dp"
        tools:text="15 results found" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerResults"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <LinearLayout
        android:id="@+id/emptyState"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/no_matches"
            android:textColor="@color/text_main"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnClearSearchEmpty"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="@string/btn_clear_search"
            android:backgroundTint="@color/action_blue"
            app:cornerRadius="24dp"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</LinearLayout>